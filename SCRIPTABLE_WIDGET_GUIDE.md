# ZOE Outage Widget для Scriptable

iPhone віджет для відображення графіків відключення електроенергії з використанням додатку [Scriptable](https://scriptable.app/).

![Widget Preview](https://img.shields.io/badge/iOS-14.0%2B-blue) ![Scriptable](https://img.shields.io/badge/Scriptable-1.7%2B-orange)

## Що це?

Це віджет для iOS, який показує:
- 📊 Ваш графік відключень електроенергії
- 💡 Поточний статус (є/немає світла)
- ⏰ Час наступного відключення/відновлення
- ⚡️ Автоматичне оновлення даних

## Вимоги

- iPhone з iOS 14.0 або новіше
- Додаток [Scriptable](https://apps.apple.com/ua/app/scriptable/id1405459188) (безкоштовний)
- Запущений ZOE API сервер (localhost або production)

## Встановлення

### Крок 1: Встановити Scriptable

1. Завантажте Scriptable з App Store
2. Відкрийте додаток

### Крок 2: Створити новий скрипт

1. Натисніть `+` у правому верхньому куті
2. Натисніть на `Untitled Script`
3. Введіть назву: `ZOE Outage Widget`
4. Скопіюйте весь код з файлу [ZoeWidget.js](ZoeWidget.js)
5. Вставте код у редактор Scriptable
6. Натисніть `Done`

### Крок 3: Налаштувати API URL

**ВАЖЛИВО:** Перед використанням віджету потрібно налаштувати URL вашого API.

Знайдіть у коді рядок:

```javascript
const CONFIG = {
  API_BASE_URL: "http://localhost:8000",  // ← Змініть це
  DEFAULT_QUEUE: "1.1",
  // ...
};
```

Змініть `API_BASE_URL` на:

#### Варіант A: Локальне тестування (тільки в додатку)

```javascript
API_BASE_URL: "http://localhost:8000"
```

⚠️ **Увага:** Локальний сервер працює тільки коли ви запускаєте скрипт в додатку Scriptable. Віджет на домашньому екрані НЕ ЗМОЖЕ підключитись до localhost.

#### Варіант B: Production сервер (рекомендовано)

Спочатку задеплойте API на один з сервісів:

**Heroku:**
```javascript
API_BASE_URL: "https://your-app-name.herokuapp.com"
```

**Railway:**
```javascript
API_BASE_URL: "https://your-app-name.railway.app"
```

**DigitalOcean:**
```javascript
API_BASE_URL: "https://your-domain.com"
```

Докладні інструкції по deployment див. в [README.md](README.md#deployment)

### Крок 4: Додати віджет на домашній екран

1. Перейдіть на домашній екран iPhone
2. Утримуйте палець на порожньому місці до появи режиму редагування
3. Натисніть `+` у верхньому лівому куті
4. Знайдіть **Scriptable** у списку
5. Виберіть бажаний розмір віджету:
   - **Small** (маленький) - компактний вигляд зі статусом
   - **Medium** (середній) - детальний графік
   - **Large** (великий) - повний графік з усіма часами

6. Натисніть **Add Widget**
7. Віджет з'явиться на домашньому екрані

### Крок 5: Налаштувати віджет

1. Утримуйте палець на віджеті
2. Натисніть **Edit Widget**
3. У полі **Script** виберіть `ZOE Outage Widget`
4. У полі **Parameter** введіть номер вашої черги, наприклад:
   - `1.1`
   - `2.1`
   - `3.2`
   - тощо

5. Натисніть за межами меню для збереження

## Використання

### Розміри віджету

#### Small Widget (Маленький)
```
⚡️ 1.1
● Є світло
💡 12:00
Оновлено: 10:30
```

Показує:
- Номер черги
- Поточний статус
- Час наступної події

#### Medium Widget (Середній)
```
⚡️ Черга 1.1        ●

Є СВІТЛО

📅 Графік на сьогодні:
🔌 03:00 - 08:00
🔌 12:00 - 17:00
🔌 21:00 - 24:00

Оновлено: 10:30
```

Показує:
- Номер черги і статус
- Поточний стан великими літерами
- Повний графік відключень

#### Large Widget (Великий)
```
⚡️ Графік відключень    [1.1]

┌─────────────────────┐
│ ПОТОЧНИЙ СТАТУС     │
│ Є СВІТЛО            │
└─────────────────────┘

⏰ Наступне відключення:
12:00

📅 Повний графік:
🔌 03:00 - 08:00
🔌 12:00 - 17:00
🔌 21:00 - 24:00

Оновлено: 10:30
```

Показує:
- Всю інформацію детально
- Виділену картку зі статусом
- Наступну подію
- Весь графік

### Індикатори статусу

- 🟢 **Зелений** - Є світло
- 🔴 **Червоний** - Немає світла

## Налаштування

### Зміна черги

Щоб змінити чергу:

1. Утримуйте палець на віджеті
2. **Edit Widget**
3. У полі **Parameter** введіть нову чергу (наприклад, `2.1`)

### Доступні черги

```
1.1, 1.2, 2.1, 2.2, 3.1, 3.2,
4.1, 4.2, 5.1, 5.2, 6.1, 6.2
```

### Кольорова тема

Ви можете змінити кольори у коді, знайшовши секцію `COLORS`:

```javascript
COLORS: {
  powerOn: new Color("#4CAF50"),      // Зелений - є світло
  powerOff: new Color("#F44336"),     // Червоний - немає світла
  background: new Color("#1E1E1E"),   // Темний фон
  textPrimary: new Color("#FFFFFF"),  // Білий текст
  textSecondary: new Color("#B0B0B0"),// Сірий текст
  cardBackground: new Color("#2D2D2D") // Фон карток
}
```

### Частота оновлення

Віджет автоматично оновлюється кожні:
- **15-30 хвилин** (залежно від налаштувань iOS)

Для примусового оновлення:
1. Видаліть віджет
2. Додайте його знову

## Кешування

Віджет автоматично зберігає останні дані в пам'яті iPhone:

- **Час життя кешу**: 30 хвилин
- **Fallback**: Якщо API недоступний, показує кешовані дані
- **Індикатор**: При використанні кешу з'являється "📦 Кешовані дані"

## Troubleshooting (Вирішення проблем)

### Віджет показує "Помилка"

**Причини:**
1. API сервер не запущений
2. Невірний URL в конфігурації
3. Немає інтернет-з'єднання
4. Використовується localhost на домашньому екрані

**Рішення:**
- Перевірте чи запущений API сервер
- Переконайтесь що URL правильний
- Використовуйте production URL, а не localhost
- Перевірте інтернет-з'єднання

### Віджет не оновлюється

**Рішення:**
1. Видаліть віджет з домашнього екрану
2. Додайте його знову
3. Перезавантажте iPhone

### Віджет показує невірну чергу

**Рішення:**
1. Утримуйте палець на віджеті
2. Edit Widget
3. Перевірте поле **Parameter**
4. Введіть правильний номер черги

### Не працює з localhost

**Пояснення:**
Віджети на домашньому екрані iOS не мають доступу до localhost з міркувань безпеки.

**Рішення:**
- Використовуйте тестування в додатку Scriptable (кнопка ▶️)
- Або задеплойте API на production сервер

## Тестування в додатку

Перед додаванням віджету на екран, протестуйте його:

1. Відкрийте Scriptable
2. Виберіть `ZOE Outage Widget`
3. Натисніть ▶️ (Run) внизу
4. Скрипт покаже preview віджету

Ви можете змінити розмір preview, змінивши рядок у коді:

```javascript
// Змініть це:
widget.presentMedium();

// На один з:
widget.presentSmall();   // Маленький
widget.presentMedium();  // Середній
widget.presentLarge();   // Великий
```

## Приклади використання

### Один віджет для домашньої черги

Створіть віджет з параметром вашої домашньої черги (`1.1`)

### Кілька віджетів для різних локацій

Створіть декілька віджетів з різними параметрами:
- Дім: `1.1`
- Робота: `2.2`
- Дача: `3.1`

## Технічні деталі

### Структура коду

- `CONFIG` - Налаштування (URL, черга, кольори)
- `createWidget()` - Головна функція створення віджету
- `buildSmallWidget()` - UI малого віджету
- `buildMediumWidget()` - UI середнього віджету
- `buildLargeWidget()` - UI великого віджету
- `fetchSchedule()` - Запит до API
- `isCurrentlyOff()` - Визначення поточного статусу
- `getNextEvent()` - Наступна подія (відключення/відновлення)
- `saveToCache()` / `loadFromCache()` - Кешування

### API Endpoints

Віджет використовує endpoint:
```
GET /api/schedules/queue/{queue_id}
```

Приклад відповіді:
```json
{
  "success": true,
  "queue_data": {
    "queue": "1.1",
    "outages": [
      {"start": "03:00", "end": "08:00"},
      {"start": "12:00", "end": "17:00"},
      {"start": "21:00", "end": "24:00"}
    ],
    "status": "active"
  },
  "cache_hit": false,
  "updated_at": "2026-01-25T11:44:00.005453"
}
```

## Обмеження

1. **Оновлення**: iOS контролює частоту оновлень віджетів (зазвичай 15-30 хвилин)
2. **Localhost**: Не працює на домашньому екрані, тільки в додатку
3. **Інтернет**: Потрібне з'єднання для отримання даних (fallback на кеш)
4. **Розмір**: Обмежений текст у малих віджетах

## Deployment API

Щоб віджет працював на домашньому екрані, потрібно задеплоїти API:

### Швидкий deployment на Railway (рекомендовано)

1. Створіть акаунт на [Railway](https://railway.app)
2. Підключіть GitHub репозиторій
3. Railway автоматично визначить Python
4. API буде доступний на URL: `https://your-app.railway.app`
5. Замініть `API_BASE_URL` у віджеті на цей URL

Докладніше: [README.md - Deployment](README.md#deployment)

## Додаткові можливості

### Інтеграція з Shortcuts

Ви можете створити Shortcuts для швидкого доступу:

1. Відкрийте Shortcuts
2. Створіть новий Shortcut
3. Додайте дію "Run Script"
4. Виберіть `ZOE Outage Widget`
5. Додайте на домашній екран

### Нотифікації (майбутня функція)

У майбутніх версіях планується додати:
- Push-нотифікації перед відключенням
- Сповіщення про зміну графіку

## FAQ

**Q: Чи можна використовувати без API?**
A: Ні, віджет потребує запущений ZOE API для отримання даних.

**Q: Чи можна налаштувати кілька черг в одному віджеті?**
A: Ні, але ви можете додати кілька віджетів з різними чергами.

**Q: Скільки займає пам'яті?**
A: ~2-5 KB на кеш файл, дуже мало.

**Q: Чи працює без інтернету?**
A: Так, якщо є кешовані дані (не старіші 30 хвилин).

**Q: Як часто оновлюються дані?**
A: iOS оновлює віджети кожні 15-30 хвилин автоматично.

## Підтримка

Якщо у вас виникли проблеми:

1. Перевірте чи правильно налаштований `API_BASE_URL`
2. Переконайтесь що API запущений і доступний
3. Перевірте чи правильно введено номер черги
4. Подивіться логи в Scriptable (Console)

## Ліцензія

MIT License

## Автор

ZOE API Project

---

**Зроблено з ❤️ для України**
