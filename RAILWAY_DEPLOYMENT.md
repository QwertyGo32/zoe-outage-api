# Railway Deployment Guide - ZOE Outage API

Покрокова інструкція для deployment ZOE API на Railway.

## Переваги Railway

- ✅ Безкоштовний tier ($5 кредитів на місяць)
- ✅ Автоматичне визначення Python
- ✅ HTTPS з коробки
- ✅ Автоматичний deploy з GitHub
- ✅ Логи в реальному часі
- ✅ Не потрібна credit card для початку

---

## Крок 1: Створити акаунт на Railway

1. Перейдіть на [railway.app](https://railway.app)
2. Натисніть **Login**
3. Увійдіть через GitHub (рекомендовано) або email

---

## Крок 2: Завантажити код на GitHub

### Варіант A: Створити новий репозиторій на GitHub

1. Перейдіть на [github.com/new](https://github.com/new)
2. Назвіть репозиторій: `zoe-outage-api`
3. Зробіть його **Public** або **Private** (на ваш вибір)
4. **НЕ** додавайте README, .gitignore або license (вони вже є)
5. Натисніть **Create repository**

### Варіант B: Підключити існуючий репозиторій

Якщо у вас вже є репозиторій на GitHub, перейдіть до Кроку 3.

---

## Крок 3: Підключити локальний репозиторій до GitHub

Відкрийте термінал у папці проекту та виконайте:

```bash
# Додати remote (замініть YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/zoe-outage-api.git

# Перейменувати гілку на main (якщо потрібно)
git branch -M main

# Завантажити код на GitHub
git push -u origin main
```

**Примітка**: Якщо потрібно авторизуватись, використовуйте GitHub Personal Access Token замість пароля.

### Створення Personal Access Token (якщо потрібно)

1. Перейдіть на [github.com/settings/tokens](https://github.com/settings/tokens)
2. **Generate new token** → **Generate new token (classic)**
3. Виберіть scope: `repo` (повний доступ до репозиторіїв)
4. **Generate token**
5. Скопіюйте token і використовуйте як пароль при git push

---

## Крок 4: Створити проект на Railway

1. Перейдіть на [railway.app/new](https://railway.app/new)
2. Натисніть **Deploy from GitHub repo**
3. Якщо це перший раз, Railway попросить доступ до GitHub - дозвольте
4. Виберіть репозиторій `zoe-outage-api`
5. Натисніть **Deploy Now**

Railway автоматично:
- ✅ Визначить що це Python проект
- ✅ Встановить залежності з `requirements.txt`
- ✅ Запустить додаток використовуючи `Procfile`
- ✅ Призначить публічний URL

---

## Крок 5: Налаштування (опціонально)

### 5.1 Встановити змінні оточення (якщо потрібно)

На даний момент API не потребує змінних оточення, але якщо в майбутньому знадобиться:

1. У проекті Railway натисніть **Variables**
2. Додайте необхідні змінні
3. Натисніть **Add** → Redeploy відбудеться автоматично

### 5.2 Налаштувати Custom Domain (опціонально)

Якщо у вас є свій домен:

1. У проекті Railway → **Settings** → **Domains**
2. Натисніть **Custom Domain**
3. Введіть ваш домен
4. Налаштуйте DNS записи згідно інструкцій Railway

---

## Крок 6: Отримати Production URL

Після завершення deployment:

1. У проекті Railway знайдіть секцію **Deployments**
2. Останній deployment має статус ✅ **Success**
3. Натисніть на deployment
4. Скопіюйте **Public URL** (наприклад: `https://zoe-outage-api-production.up.railway.app`)

**Це ваш production URL!** 🎉

---

## Крок 7: Перевірити deployment

Відкрийте у браузері:

```
https://your-app-name.up.railway.app/health
```

Повинна бути відповідь:
```json
{
  "status": "healthy",
  "timestamp": "2026-02-02T...",
  "version": "1.0.0"
}
```

### Перевірити документацію API

```
https://your-app-name.up.railway.app/docs
```

Відкриється Swagger UI з усіма endpoints.

### Перевірити основні endpoints

**Отримати графік для черги 1.1:**
```
https://your-app-name.up.railway.app/api/schedules/queue/1.1
```

**Отримати список черг:**
```
https://your-app-name.up.railway.app/api/queues
```

---

## Крок 8: Оновити Scriptable Widget

1. Відкрийте [ZoeWidget.js](ZoeWidget.js)
2. Знайдіть рядок:

```javascript
const CONFIG = {
  API_BASE_URL: "http://localhost:8000",  // ← Змініть це
  // ...
};
```

3. Замініть на ваш Railway URL:

```javascript
const CONFIG = {
  API_BASE_URL: "https://your-app-name.up.railway.app",  // ← Ваш URL
  // ...
};
```

4. Збережіть файл
5. Скопіюйте оновлений код у Scriptable на iPhone
6. Віджет тепер працюватиме з production API! 🎉

---

## Автоматичний Deploy

Railway автоматично робить redeploy кожен раз коли ви push код на GitHub:

```bash
# Внести зміни в код
git add .
git commit -m "Update: ваші зміни"
git push

# Railway автоматично задеплоїть нову версію
```

Ви можете стежити за процесом deployment в Railway dashboard.

---

## Моніторинг та Логи

### Переглянути логи

1. У проекті Railway → **Deployments**
2. Натисніть на активний deployment
3. Перегляньте **Logs** в реальному часі

### Метрики

Railway показує:
- CPU usage
- Memory usage
- Network traffic
- Request count

Все це доступно в Dashboard.

---

## Troubleshooting

### Deployment failed

**Перевірте логи:**
1. Railway Dashboard → Deployments → Logs
2. Знайдіть помилку

**Типові помилки:**
- Відсутні залежності в `requirements.txt` - додайте потрібні пакети
- Синтаксична помилка в коді - перевірте код локально
- PORT не налаштовано - переконайтесь що використовується `$PORT`

### API не відповідає

**Перевірте:**
1. Чи deployment успішний? (зелена галочка ✅)
2. Чи правильний URL?
3. Спробуйте `/health` endpoint спочатку

### Віджет не працює

**Перевірте:**
1. Чи правильно вказаний `API_BASE_URL` у віджеті?
2. Чи використовується `https://` (не `http://`)?
3. Чи є інтернет-з'єднання на iPhone?
4. Перевірте логи в Scriptable Console

---

## Безкоштовний Tier

Railway надає:
- **$5 кредитів на місяць** безкоштовно
- Це достатньо для:
  - 500+ годин роботи сервера
  - Тисячі API запитів
  - Невелике особисте використання

Для більшого навантаження:
- Додайте credit card
- Платите тільки за те, що використовуєте
- ~$5-10/місяць для середнього навантаження

---

## Корисні команди

### Локальне тестування перед deploy

```bash
# Запустити локально
python main.py

# Або через uvicorn
uvicorn main:app --reload --port 8000

# Перевірити
curl http://localhost:8000/health
```

### Git команди

```bash
# Подивитись статус
git status

# Подивитись історію
git log --oneline

# Створити новий коміт
git add .
git commit -m "Ваше повідомлення"
git push
```

---

## Наступні кроки

Після успішного deployment:

1. ✅ Протестувати всі endpoints на production
2. ✅ Оновити віджет з production URL
3. ✅ Додати віджет на домашній екран iPhone
4. ✅ Протестувати віджет з реальним API

Додаткові покращення (опціонально):
- [ ] Додати моніторинг (Sentry, LogRocket)
- [ ] Налаштувати custom domain
- [ ] Додати rate limiting
- [ ] Створити CI/CD pipeline
- [ ] Додати automated tests

---

## Підтримка

**Railway документація:**
- [Docs](https://docs.railway.app)
- [Community](https://discord.gg/railway)

**Проект документація:**
- [README.md](README.md) - Загальна інформація
- [SCRIPTABLE_WIDGET_GUIDE.md](SCRIPTABLE_WIDGET_GUIDE.md) - Гайд по віджету
- [API_TESTING_RESULTS.md](API_TESTING_RESULTS.md) - Результати тестування

---

## Готово! 🎉

Тепер ваш ZOE Outage API працює на production і доступний з будь-якої точки світу!

Скопіюйте ваш Railway URL та оновіть віджет - і все готово до використання на iPhone.
